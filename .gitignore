# dependencies
node_modules/

# pnpm local store (project-internal cache)
.pnpm-store/

# local debug artifacts (browser profiles, dumps, screenshots)
.logs/

# npm pack output
*.tgz

# npm local cache (used when publishing from the sandbox)
.npm-cache/

# local-only release checklist (not tracked)
RELEASE.md

# internal requirements/design notes (not tracked — not part of the published repo)
dsh-session-buddy-REQUIREMENTS.md

# TypeScript incremental build info (machine-local cache)
lib/tsconfig.tsbuildinfo

# git-manager skill local config (may hold credentials)
.cqy-skills-setting/

# OS files
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
